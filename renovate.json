{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "local>kanso-labs/.github:renovate-config is the organization's shared Renovate preset, and it is listed last because a later preset wins a conflict with an earlier one. This file is merged over config.js rather than inheriting from it, and config:recommended reinstates whatever config.js disabled, so extending the shared preset here is what actually carries an organization-wide setting into this repository. The extends array is therefore ordered by precedence rather than by name, unlike the keys around it.",
  "extends": [
    "config:recommended",
    "customManagers:githubActionsVersions",
    "local>kanso-labs/.github:renovate-config"
  ],
  "packageRules": [
    {
      "automerge": true,
      "description": "Everything updated here is CI tooling that the Validate workflow runs on the pull request that bumps it, so a broken version fails a required check rather than reaching main. Majors are left out: the one that matters is renovatebot/github-action, and a major of the thing that updates every managed repository is worth a person looking at. Stated in full rather than leaning on the patch-only rule in config.js, so this does not depend on whether packageRules concatenate or replace when the global config is merged with this one.",
      "matchUpdateTypes": ["minor", "patch"]
    }
  ]
}
